apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
bases:
- ../../base

configMapGenerator:
- name: solmar-env-vars
  files:
  - env-vars.properties

patchesStrategicMerge:
- image-tags.yaml
- env-patch.yaml